# EBSPPT — 시험 PPT 제작 프로세스

시험 주제를 받아 PPT 기획 + AI 키비주얼을 만드는 5단계 파이프라인.
이 프로젝트에서 작업할 때 아래 **흐름과 체크포인트를 반드시 따른다.**

---

## 전체 파이프라인
| 단계 | 작업 | 담당 |
|------|------|------|
| 1 | 디자인 기획 (브리프) | 여기 (Claude) |
| 2 | PPT 구성 (슬라이드 + 이미지 목록) | 여기 |
| 3 | 키비주얼 생성 (Higgsfield) | 여기 |
| 4 | PPT 초안 추출 | NotebookLM (사용자) |
| 5 | 최종 수정·제출 | Canva (사용자) |

→ **여기서는 1~3단계만** 수행하고, 결과(기획 + 이미지 매핑)를 사용자에게 넘긴다.

## 모드 (주제 성격에 따라 1개 선택)
| 모드 | 언제 | 3단계 생성 |
|------|------|-----------|
| `character` | 캐릭터 제안 요구 | Soul 학습 → 캐릭터 컷 |
| `video` | 영상 제작 요구 (실제 영상 X, **키프레임 스틸**) | 키프레임 이미지 |
| `default` | 그 외 | 정적 키이미지 |

프롬프트 파일: `prompts/<모드>/01-design-brief.md` · `02-ppt-structure.md` · `03-keyvisual.md`
(각 파일의 `프롬프팅 영역 (작성)` 칸이 사용자가 정한 규칙. 채워져 있으면 그대로 따른다.)

---

## 실행 흐름 & 체크포인트
> ★ = **반드시 멈추고 사용자에게 물어본 뒤** 다음으로 진행. (켜고 끄려면 ★를 지우거나 추가)

1. **주제 수신** → 모드 판단.
   - 모드가 애매하면 물어본다. 명확하면 바로 진행.
2. **1단계 디자인 브리프** 작성 → **★ CP1: 브리프 보여주고 "이대로 2단계 갈까?" 확인.**
3. **2단계 PPT 구성** 작성 (슬라이드 구성표 + 생성할 이미지 목록) → **★ CP2: 목록 보여주고 "이대로 이미지 생성할까?" 확인. (크레딧 쓰기 직전 — 필수)**
4. **3단계 키비주얼 생성**
   - `character`/`default`: 히어로 컷 먼저 → **★ CP3: 옵션 보여주고 선택 받기.**
   - `character`: Soul 학습 전 → **★ CP4: "Soul 학습 시작할까? (~10분)" 확인.**
   - 확정 후 슬라이드별 컷/이미지 생성.
5. **최종 전달**: 결과물을 **`outputs/<주제-슬러그>/` 폴더에 파일로 정리**한다. (채팅 텍스트만으로 끝내지 않음)
   - `README.md` (요약·메타) · `01-design-brief.md` · `02-ppt-structure.md` · `03-keyvisual-map.md`
   - `images/` 에 생성 이미지를 **실제 다운로드**(raw URL → PowerShell Invoke-WebRequest), 학습 레퍼런스는 `images/refs/`
   - 슬라이드 컷 파일명: `slide-<번호>-<라벨>.png`

## 생성 규칙 (기본값)
- 기본 모델: `nano_banana_pro` (캐릭터 슬라이드 컷은 `soul_2` + soul_id)
- 모든 이미지는 1단계 브리프의 colors/style/tone을 일관되게 주입한다.
- Soul 학습: 같은 캐릭터 레퍼런스 5~20장 필요 → 히어로 컷을 레퍼런스로 변형 생성해 확보.
- 이미지 잡 ID가 Soul 학습에서 거부되면 https URL로 전달한다.
